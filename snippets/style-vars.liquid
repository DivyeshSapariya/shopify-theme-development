{%- comment -%}
  snippets/style-vars.liquid
  ------------------------------------------------------------------
  This is the bridge: later, real section settings will flow through
  here and come out as CSS variables. For THIS step, everything is
  hardcoded on purpose — we're only testing that the snippet renders
  correctly and can be called from a section. Real settings get wired
  in during the next step (building hero.liquid).
  ------------------------------------------------------------------
{%- endcomment -%}
--pt: 20px;
--pb: 20px;
--bg: #fceec1;
