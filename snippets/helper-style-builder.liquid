{% comment %}
  Deprecated — use helper-var-attribute.liquid for inline CSS variables.
  Kept for reference; outputs scoped style block if still needed.
{% endcomment %}
{%- if variables != blank and selector != blank -%}
<style>
  {{ selector }} {
    {{ variables | strip }}
  }
</style>
{%- endif -%}
